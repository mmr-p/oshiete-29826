<div class="chat-sender"><%= message.user.name %></div>
<div class="chat-text"><%= simple_format(h message.content) %></div>
<div class="chat-date"><%= message.created_at.strftime('%Y/%m/%d %H:%M') %></div>
